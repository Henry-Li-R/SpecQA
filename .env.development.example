# Local development overrides. Copy to .env.development (which is git-ignored).
# Weaviate Cloud free trial has ended — use local Docker Weaviate instead.
# These values override .env when run_eval.py / update_baselines.py load dotenv.
WEAVIATE_URL=http://localhost:8080
WEAVIATE_GRPC_PORT=50051
WEAVIATE_API_KEY=
